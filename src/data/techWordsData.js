/** @typedef {'networking'|'cloud'|'devops'|'kubernetes'|'security'|'cicd'|'linux'|'general'} TechWordCategoryId */

import { TECH_WORDS_DNS_RECORDS, TECH_WORDS_JOB_LISTING } from './techWordsJobListing.js'

export const TECH_WORD_CATEGORIES = [
  { id: 'all', label: 'All' },
  { id: 'networking', label: 'Networking' },
  { id: 'cloud', label: 'Cloud' },
  { id: 'devops', label: 'DevOps' },
  { id: 'kubernetes', label: 'Kubernetes' },
  { id: 'security', label: 'Security' },
  { id: 'cicd', label: 'CI/CD' },
  { id: 'linux', label: 'Linux' },
  { id: 'general', label: 'General' },
]

export const TECH_WORD_CATEGORY_IDS = new Set(
  TECH_WORD_CATEGORIES.filter((c) => c.id !== 'all').map((c) => c.id)
)

/**
 * @type {Array<{
 *   id: string
 *   term: string
 *   categoryId: TechWordCategoryId
 *   shortDefinition: string
 *   whyWeUseIt: string
 *   whereUsed: string
 *   example: string
 *   snippet?: string
 *   beginnerExplanation?: string
 * }>}
 */
const TECH_WORDS_BASE = [
  {
    id: 'dns',
    term: 'DNS',
    categoryId: 'networking',
    shortDefinition: 'Maps human-readable hostnames to IP addresses so clients can reach services without memorizing numbers.',
    whyWeUseIt: 'Stable names survive IP changes; enables load distribution and global routing via records like CNAME and geo-DNS.',
    whereUsed: 'Browsers, CDNs, Kubernetes Services (CoreDNS), service discovery, TLS certificate issuance (ACME).',
    example: 'An A record points api.example.com to 203.0.113.10; a CNAME aliases www to a CDN hostname.',
    snippet: 'dig +short api.example.com A',
    beginnerExplanation: 'Like a phone book for the internet: you type a name, DNS tells your computer which address to call.',
  },
  {
    id: 'subnet',
    term: 'Subnet',
    categoryId: 'networking',
    shortDefinition: 'A segmented range of IP addresses inside a larger network, isolated for routing and policy.',
    whyWeUseIt: 'Limits blast radius, applies firewall rules per tier, and reserves address space for workloads.',
    whereUsed: 'VPCs, on-prem VLANs, Kubernetes pod CIDRs, database private subnets.',
    example: 'A /24 subnet 10.0.1.0/24 might hold only application servers behind a single route table.',
    beginnerExplanation: 'A smaller “neighborhood” of addresses inside your bigger network so traffic rules stay clear.',
  },
  {
    id: 'load-balancer',
    term: 'Load Balancer',
    categoryId: 'networking',
    shortDefinition: 'Distributes client traffic across healthy backend targets to improve capacity and availability.',
    whyWeUseIt: 'Avoids single points of failure, enables rolling updates, and can terminate TLS at the edge.',
    whereUsed: 'ALB/NLB, GCP LB, Azure LB, Ingress controllers, service meshes.',
    example: 'An ALB sends HTTPS traffic to three EC2 instances in different AZs based on least connections.',
    beginnerExplanation: 'A traffic director that spreads visitors across many servers so none gets overwhelmed.',
  },
  {
    id: 'vpc',
    term: 'VPC',
    categoryId: 'cloud',
    shortDefinition: 'An isolated virtual network in a public cloud where you define subnets, routes, and security boundaries.',
    whyWeUseIt: 'Mirrors on-prem segmentation with IAM and network ACLs; foundation for private workloads.',
    whereUsed: 'AWS VPC, GCP VPC, Azure VNet; hosts subnets for EKS/GKE/AKS nodes.',
    example: 'Three subnets across two AZs: public for ingress, private for apps, private for RDS.',
    beginnerExplanation: 'Your own private slice of the cloud network that you design and lock down.',
  },
  {
    id: 'iam',
    term: 'IAM',
    categoryId: 'cloud',
    shortDefinition: 'Identity and access management: who can do what on which resources, enforced by the cloud provider.',
    whyWeUseIt: 'Least privilege reduces breach impact; federated roles integrate SSO and CI/CD identities.',
    whereUsed: 'AWS IAM, GCP IAM, Azure RBAC; IRSA/workload identity for pods.',
    example: 'A CI role may only push to ECR repo X; a pod SA may only read secret Y.',
    beginnerExplanation: 'The bouncer list: which people and machines are allowed to touch which cloud buttons.',
  },
  {
    id: 'auto-scaling',
    term: 'Auto Scaling',
    categoryId: 'cloud',
    shortDefinition: 'Adds or removes capacity based on metrics or schedules to match demand.',
    whyWeUseIt: 'Cost control during quiet hours and reliability during spikes without manual resizing.',
    whereUsed: 'ASG, GKE node pools, Karpenter, serverless concurrency limits.',
    example: 'Scale out when CPU > 70% for 5 minutes; scale in when below 30% with cooldown.',
    beginnerExplanation: 'Automatically hires and releases servers as traffic goes up and down.',
  },
  {
    id: 'api-gateway',
    term: 'API Gateway',
    categoryId: 'cloud',
    shortDefinition: 'Managed entry point for HTTP APIs with auth, throttling, routing, and observability.',
    whyWeUseIt: 'Centralizes cross-cutting concerns so services stay small and policies stay consistent.',
    whereUsed: 'Amazon API Gateway, Azure API Management, Apigee, Kong, Envoy at edge.',
    example: 'JWT validation, rate limit 1000 rps per key, route /orders to Lambda and /users to ECS.',
    beginnerExplanation: 'One front door for your APIs with keys, limits, and logging built in.',
  },
  {
    id: 'cicd-term',
    term: 'CI/CD',
    categoryId: 'devops',
    shortDefinition: 'Continuous Integration merges and tests code often; Continuous Delivery deploys releasable builds reliably.',
    whyWeUseIt: 'Short feedback loops catch defects early; repeatable pipelines reduce “works on my machine.”',
    whereUsed: 'GitHub Actions, GitLab CI, Jenkins, Argo CD, Flux.',
    example: 'On push: lint, unit tests, build image, scan, deploy to staging; tag triggers production.',
    beginnerExplanation: 'Robots that automatically build, test, and ship your code every time you commit.',
  },
  {
    id: 'jenkins',
    term: 'Jenkins',
    categoryId: 'devops',
    shortDefinition: 'Open-source automation server that runs pipelines as jobs with plugins and agents.',
    whyWeUseIt: 'Mature ecosystem for legacy stacks; self-hosted control when SaaS CI is not an option.',
    whereUsed: 'On-prem build farms, Kubernetes Jenkins operators, shared platform teams.',
    example: 'Declarative pipeline: checkout SCM, docker.build, push to registry, deploy via kubectl.',
    snippet: 'pipeline { agent any; stages { stage("Build") { sh "mvn package" } } }',
    beginnerExplanation: 'A popular robot boss that runs your build scripts on demand or on a schedule.',
  },
  {
    id: 'gitops',
    term: 'GitOps',
    categoryId: 'devops',
    shortDefinition: 'Desired state lives in Git; controllers reconcile clusters to match those commits.',
    whyWeUseIt: 'Audit trail, rollbacks via revert, and separation between app code and cluster state.',
    whereUsed: 'Argo CD, Flux, Terraform + VCS workflows.',
    example: 'Merge PR updating manifests/; Argo sync applies new Deployment image tag to prod.',
    beginnerExplanation: 'Treat Git as the single truth for how production should look; machines make it so.',
  },
  {
    id: 'pipeline',
    term: 'Pipeline',
    categoryId: 'devops',
    shortDefinition: 'Ordered stages (build, test, deploy) executed as a workflow with shared artifacts.',
    whyWeUseIt: 'Encodes release policy once; parallel stages shorten wall-clock time.',
    whereUsed: 'Every major CI platform; DAGs for complex dependencies.',
    example: 'Stage A builds jar; stages B/C run integration tests in parallel; stage D promotes artifact.',
    beginnerExplanation: 'An assembly line for software: each step hands something to the next.',
  },
  {
    id: 'pod',
    term: 'Pod',
    categoryId: 'kubernetes',
    shortDefinition: 'Smallest deployable unit: one or more containers sharing network and storage namespaces.',
    whyWeUseIt: 'Co-located sidecars (proxy, logger) and ephemeral groups without tight coupling.',
    whereUsed: 'Kubernetes workloads; CronJobs and Deployments create Pods.',
    example: 'A Pod runs app container + envoy sidecar; both see localhost on shared loopback via mesh config.',
    snippet: 'kubectl get pods -n prod',
    beginnerExplanation: 'A wrapper that runs one or more containers together like roommates sharing an apartment.',
  },
  {
    id: 'deployment',
    term: 'Deployment',
    categoryId: 'kubernetes',
    shortDefinition: 'Controller that rolls out ReplicaSets to maintain desired Pod count and strategy (RollingUpdate).',
    whyWeUseIt: 'Declarative updates with health checks reduce downtime during releases.',
    whereUsed: 'kube-controller-manager; GitOps applies Deployment YAML.',
    example: 'Change image tag; Deployment creates new RS, scales up new Pods, drains old ones.',
    snippet: 'kubectl rollout status deployment/api -n prod',
    beginnerExplanation: 'Tells Kubernetes “keep this many copies of my app running” and upgrades them safely.',
  },
  {
    id: 'service',
    term: 'Service',
    categoryId: 'kubernetes',
    shortDefinition: 'Stable virtual IP and DNS name that load-balances traffic to matching Pods via selectors.',
    whyWeUseIt: 'Pods are ephemeral; Services provide discovery and internal LB.',
    whereUsed: 'ClusterIP for east-west, NodePort/LoadBalancer for north-south.',
    example: 'Service checkout selects app=checkout; CoreDNS resolves checkout.prod.svc.cluster.local.',
    beginnerExplanation: 'A permanent front desk name that always reaches whichever pods are healthy.',
  },
  {
    id: 'ingress',
    term: 'Ingress',
    categoryId: 'kubernetes',
    shortDefinition: 'HTTP/S routing rules from outside the cluster to Services (host/path, TLS).',
    whyWeUseIt: 'Single entry with certificates and canary splits instead of many LoadBalancers.',
    whereUsed: 'nginx-ingress, ALB controller, Gateway API implementations.',
    example: 'Host shop.example.com routes /api/* to api-svc and /* to web-svc with cert-manager TLS.',
    snippet: 'kubectl describe ingress shop -n prod',
    beginnerExplanation: 'The traffic cop at the cluster edge that sends web requests to the right internal service.',
  },
  {
    id: 'encryption',
    term: 'Encryption',
    categoryId: 'security',
    shortDefinition: 'Transforms data so only parties with keys can read it—at rest and in transit.',
    whyWeUseIt: 'Meets compliance, limits insider risk, and protects credentials and PII.',
    whereUsed: 'TLS, KMS envelopes, disk encryption, etcd secrets encryption.',
    example: 'S3 bucket default SSE-KMS; TLS 1.3 between client and Ingress; sealed secrets in Git.',
    beginnerExplanation: 'Scrambling data with math so eavesdroppers only see gibberish without the key.',
  },
  {
    id: 'oauth',
    term: 'OAuth',
    categoryId: 'security',
    shortDefinition: 'Delegated authorization framework; users consent without sharing passwords with third parties.',
    whyWeUseIt: 'SSO across SaaS; scoped tokens for APIs; OIDC adds identity layer on top.',
    whereUsed: 'Login with Google/GitHub, service-to-service with client credentials.',
    example: 'SPA obtains access token for audience api.example.com with refresh rotation.',
    beginnerExplanation: 'Let another app act on your behalf with a limited hall pass, not your real password.',
  },
  {
    id: 'rbac',
    term: 'RBAC',
    categoryId: 'security',
    shortDefinition: 'Role-Based Access Control maps identities to roles with permissions on resources.',
    whyWeUseIt: 'Auditable, scalable authz compared to per-user ACL sprawl.',
    whereUsed: 'Kubernetes RBAC, cloud IAM bindings, app-level roles.',
    example: 'Role pod-reader binds to Group sre; ClusterRole deployer limited to namespace staging.',
    snippet: 'kubectl auth can-i list pods --as=system:serviceaccount:demo:ci',
    beginnerExplanation: 'Job titles for permissions: “developer” can deploy here; “auditor” can only read.',
  },
  {
    id: 'build',
    term: 'Build',
    categoryId: 'cicd',
    shortDefinition: 'Compile, bundle, or containerize source into an artifact ready to test or deploy.',
    whyWeUseIt: 'Reproducible binaries/images shrink “drift” between environments.',
    whereUsed: 'docker build, mvn package, npm run build, Bazel, Kaniko in cluster.',
    example: 'Multi-stage Dockerfile: builder stage compiles Go binary; runtime stage is distroless.',
    snippet: 'docker build -t api:1.4.2 -f Dockerfile .',
    beginnerExplanation: 'Turning your source code into something you can actually run or ship.',
  },
  {
    id: 'artifact',
    term: 'Artifact',
    categoryId: 'cicd',
    shortDefinition: 'Immutable output of a build—jar, OCI image, helm chart—that flows through the pipeline.',
    whyWeUseIt: 'Promote the same bits across stages; sign and scan once.',
    whereUsed: 'ECR, GCR, Artifactory, GitHub Packages.',
    example: 'Pipeline uploads api@sha256:abc…; prod deploy references digest, not :latest.',
    beginnerExplanation: 'The packaged result of a build, versioned and stored like a numbered release box.',
  },
  {
    id: 'runner',
    term: 'Runner',
    categoryId: 'cicd',
    shortDefinition: 'Agent that executes CI jobs—VM, container, or hosted pool with labels and secrets.',
    whyWeUseIt: 'Isolates untrusted build code; scales horizontally for queue depth.',
    whereUsed: 'GitHub Actions runners, GitLab runners, Jenkins agents.',
    example: 'Self-hosted runner on GKE with label gpu=true for ML training jobs only.',
    beginnerExplanation: 'The worker machine that actually runs your pipeline steps when the scheduler asks.',
  },
  {
    id: 'cron-job',
    term: 'Cron Job',
    categoryId: 'linux',
    shortDefinition: 'Schedules commands using cron expressions on a fixed clock.',
    whyWeUseIt: 'Backups, certificate renewals, report generation without long-running daemons.',
    whereUsed: 'crontab, systemd timers, Kubernetes CronJob.',
    example: '0 3 * * * /usr/local/bin/backup.sh >> /var/log/backup.log 2>&1',
    snippet: 'crontab -e',
    beginnerExplanation: 'A timer that runs a script automatically at times you specify.',
  },
  {
    id: 'daemon',
    term: 'Daemon',
    categoryId: 'linux',
    shortDefinition: 'Background process providing continuous service without an interactive terminal.',
    whyWeUseIt: 'Servers (sshd), init systems, log collectors run as daemons.',
    whereUsed: 'systemd units, container entrypoints supervising workers.',
    example: 'systemd manages docker.service as a long-running daemon with restart policies.',
    beginnerExplanation: 'A program that stays invisible in the background doing its job 24/7.',
  },
  {
    id: 'shell-script',
    term: 'Shell Script',
    categoryId: 'linux',
    shortDefinition: 'Executable file of shell commands for automation, glue, and bootstrapping.',
    whyWeUseIt: 'Fast iteration for ops tasks; often wrapped later by Ansible/Terraform.',
    whereUsed: 'User-data, CI setup steps, local dev scripts.',
    example: '#!/usr/bin/env bash\nset -euo pipefail\napt-get update && apt-get install -y nginx',
    snippet: 'chmod +x ./provision.sh && ./provision.sh',
    beginnerExplanation: 'A text file of terminal commands the computer runs in order, like a recipe.',
  },
  {
    id: 'api',
    term: 'API',
    categoryId: 'general',
    shortDefinition: 'Application Programming Interface: contracts (often HTTP/JSON) for programs to call each other.',
    whyWeUseIt: 'Decouples teams; enables mobile/web/clients to share one backend.',
    whereUsed: 'REST, gRPC, GraphQL, cloud control planes.',
    example: 'GET /v1/users/42 returns JSON; clients use OpenAPI spec to generate SDKs.',
    beginnerExplanation: 'A menu of operations one program exposes for other programs to use safely.',
  },
  {
    id: 'microservices',
    term: 'Microservices',
    categoryId: 'general',
    shortDefinition: 'Architecture decomposing a system into independently deployable services with bounded contexts.',
    whyWeUseIt: 'Team autonomy and scaling hot paths—at the cost of distributed complexity.',
    whereUsed: 'Kubernetes fleets, service meshes, event-driven choreography.',
    example: 'Checkout service owns orders DB; inventory service publishes stock events via Kafka.',
    beginnerExplanation: 'Many small apps talking to each other instead of one giant program doing everything.',
  },
]

export const TECH_WORDS = [...TECH_WORDS_BASE, ...TECH_WORDS_JOB_LISTING, ...TECH_WORDS_DNS_RECORDS]

export function categoryLabelForTechWord(categoryId) {
  const c = TECH_WORD_CATEGORIES.find((x) => x.id === categoryId)
  return c?.label ?? categoryId
}
