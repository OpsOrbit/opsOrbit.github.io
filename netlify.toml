# Netlify — use when this folder is the Git repo root.
# If the repo root is the parent folder, use ../netlify.toml (base = "devops-commands-react").

[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
